package org.example.Operators;

public class Main {
    //Operators
    //unary operators
    //increment decrement
    //binary operators
    //arithmetic operator(+ - * / %) a+b
    //relational operators(< > <= >=)
    //equality operators(== !=)
    //logical operators(and(&&) or(||) not(!)) !true
    //bitwise operators(and(&) or(|) xor(^) compliment(~)) ~12
    //shift operators(left shift(<<) right shift(>>) unsigned right shift(>>>))
    //assignment operators(=) a+=b;
    //ternary operators

    //
}
