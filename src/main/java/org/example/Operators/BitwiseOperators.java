package org.example.Operators;

import java.sql.SQLOutput;

public class BitwiseOperators {
    //and(&) TT=T FT=F TF=F FF=F
    //or(|) TT=T FT=T TF=T FF=F
    //xor(^) TT=F FT=T TF=T FF=F
    //when both operand are different then true otherwise false
    //compliment(~) ->only used with integral values ~x= -(x+1)
    //invert all the bits
    //allowed with boolean as well as integral values

    public static void main(String[] args) {

    }
}
